language: node_js
node_js:
  - "10"
script: npm run lint && npm run build
deploy:
  provider: pages
  local-dir: www
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  committer-from-gh: true
  on:
    branch: master
